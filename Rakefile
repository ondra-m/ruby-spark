#-*- mode: ruby -*-

require "bundler/gem_tasks"
# require "maven/ruby/tasks"

# task :default => [ :test ]

# desc "run all tests"
# task :all do
#   maven.verify
# end

namespace :spark do
  desc "Get a SPARK"
  task :get do



  end

  desc "Build a ruby extension"
  task :build_ext do
    
  end
end
